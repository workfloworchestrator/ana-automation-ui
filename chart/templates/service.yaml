apiVersion: v1
kind: Service
metadata:
  name: {{ include "nsi-mgmt-info.fullname" . }}
  labels:
    {{- include "nsi-mgmt-info.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    {{- include "nsi-mgmt-info.selectorLabels" . | nindent 4 }}
