apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "nsi-mgmt-info.fullname" . }}
  labels:
    {{- include "nsi-mgmt-info.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "nsi-mgmt-info.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "nsi-mgmt-info.selectorLabels" . | nindent 8 }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
          {{- with .Values.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
